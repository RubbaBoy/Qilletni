import "string.ql"

// Prints a string representation of the given object to the console
// obj may be of any type
native fun print(obj)

// Generates a random integer between an inclusive lower bound and an exclusive upper bound
// min and max must both be integers
native fun random(min, max)
