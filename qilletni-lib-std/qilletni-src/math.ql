/**
 * Generates a random integer between an inclusive lower bound and an exclusive upper bound.
 * 
 * @param[@type int] min The inclusive minimum range of the random number
 * @param[@type int] max The exclusive maximum range of the random number
 * @returns[@type int] A random integer between the given range
 */
native fun random(min, max)
